import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SelectSlot2Page } from './select-slot2.page';

describe('SelectSlot2Page', () => {
  let component: SelectSlot2Page;
  let fixture: ComponentFixture<SelectSlot2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SelectSlot2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
