import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.compensar.velvetblade',
  appName: 'Velvet & Blade',
  webDir: 'www',
};

export default config;
